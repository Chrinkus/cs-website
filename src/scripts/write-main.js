const writeFile     = require("./node-wrappers/write-file");
const composeMain   = require("./compose-main");

function writeMainHTML(config, HTMLTemp, postHeads) {
    "use strict";
}

module.exports = writeMainHTML;
